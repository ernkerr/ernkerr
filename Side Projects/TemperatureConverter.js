// get input from user ( either F or C )

// F to C
let tempInF = 0; // change to input from user

let OutputTempInC = (5 / 9) * (tempInF - 32);

// C to F
let tempInC = 32; // change to input from user

let OutputTempInF = (5 / 9) * (tempInC + 32);

// if input = F then fahrenheitToCelcius
// else if input = C then celciusToFahrenheit
